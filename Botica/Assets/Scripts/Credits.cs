using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Credits : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    void Update()
    {
        /*if (SceneManager.GetActiveScene().name == "Credits")
        {
            GetChild().transform.MoveTowards(GetChild().transform.position, posicionFinalCliente, TextSpeed * Time.deltaTime, Space.World);
            ouroborosImage.transform.Rotate(new Vector3(0, 0, 1) * Time.deltaTime * ouroborosSpeed, Space.World);

            transform.Translate(Vector3.up * Time.deltaTime, Space.World);
        }
        */
    }
}
